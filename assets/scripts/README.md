# Active EN dialogue scripts

**Shipping inject (gold path):** Manaka (`t*`) and common (`p*`) XML here → `rebuild_dbin2/` → CIA.

**WIP:** Volunteer Nene (`a*`) / Rinko (`k*`) XML may also land here after ingest. They are **not** on the gold inject path until you promote them into `rebuild_dbin2` / inject policy. Until then the patch still uses NLPPATCH + JP ROM for those stems.

Former `assets/scripts_deferred/` (machine/community EN reference) was **removed** 2026-09-03.
